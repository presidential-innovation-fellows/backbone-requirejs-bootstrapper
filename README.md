# Backbone and RequireJS Bootstrapper   

Contains Backbone, RequireJS, Bootstrap, uses Bower, compatible with Grunt, and is organized into a tight directory structure.  CSS Framework has yet to be included.